#include <iostream>

int manhattanDistance(uint8_t * , uint8_t * , int );
bool operator<(Values & , Values & );
vector <int> calculateDistances(uint8_t *, int,std::vector<uint8_t *>, int);
int hammingDistance(int , int );
int exists(Values * , int  , int );